# Dependencies for the identity example ONLY — NOT part of the published
# allus-company-data SDK package (the SDK's pyproject packages only src/).
#
#   - the local SDK under test, installed editable (path repo, like PHP's path repo)
#   - Authlib: a standard, well-known OIDC client for scenarios 5/6 (#314 compliance
#     demo). Supports discovery, PKCE S256, client_secret_post, issuer override, and
#     id_token verification — sessionless, exactly what the demo needs.
-e ../..
authlib>=1.3
requests>=2.31
